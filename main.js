// searching find indexoff incude

const fruits = ['apple', 'banana', 'cherry', 'date', 'elderberry'];
// find the first fruit with length less than 6
const newfruits = fruits.find((fruit) => fruit.length < 6);
console.log(newfruits);
